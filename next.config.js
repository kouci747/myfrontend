/** @type {import('next').NextConfig} */
module.exports = {
  images: {
    domains: ['links.papareact.com', 's1.best-wallpaper.net'], //specifies which domains we should accept images from
  },
  reactStrictMode: true,
};
